type Module {
  # User Prompting
  speak(message: String!): String!
  ask(message: String!): String!

  # Completion Callbacks
  onGoalAchieved(message: String!): Boolean!
  onGoalFailed(message: String!): Boolean!
}
